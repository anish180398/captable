/**
 * Set to true only when SENTRY_ORG, SENTRY_PROJECT, and NEXT_PUBLIC_SENTRY_DSN are configured.
 * Disabled for now to avoid prod errors from missing env.
 */
export const isSentryEnabled = false;
